This analysis assigns a color and texture to each unique combination of cell-level formatting attributes in the underlying excel file. It considers, font, font size, text emphasis (bold, italic, underline), and optionally the data format. The most frequent format will have a white background.
